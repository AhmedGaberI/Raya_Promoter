import 'package:flutter/material.dart';

class CartNotFound extends StatelessWidget {
  const CartNotFound({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}